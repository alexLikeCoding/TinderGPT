USE_LANGUAGE=
CITY=
# personality is short description about you (3-4 sentences). Write here your hobbies or personality type.
PERSONALITY=

# ── LLM config (auto-read from ~/.claude/settings.json — override below if needed) ──
# OPENAI_API_KEY=   # auto-detected from Claude config ANTHROPIC_AUTH_TOKEN
# OPENAI_BASE_URL=  # auto-converted from DeepSeek Anthropic → OpenAI endpoint
# OPENAI_MODEL=     # default: deepseek-chat
PUSHBULLET_API_KEY=
